/*import 'package:flutter_test/flutter_test.dart';
import 'package:flame/game.dart';
import 'package:my_first_flame_game/my_game.dart';

void main() {
  testWidgets('Game starts without errors', (WidgetTester tester) async {
    await tester.pumpWidget(GameWidget(game: MyGame()));

    expect(find.byType(GameWidget), findsOneWidget);
  });
}
*/